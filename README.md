### Prerequisites
* Install project root
  ```sh
  npm install
  ```
* Install backend
  ```sh
  cd back && npm install
  ```
* Install frontend
  ```sh
  cd front && npm install
  ```

* Run the project
  ```sh
  npm run start
  ```